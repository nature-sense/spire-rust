{
  "name": "spire-rust",
  "private": true,
  "scripts": {
    "build:rust": "cd core && cargo build --release",
    "build:ts": "cd extension && npm run compile",
    "build": "pnpm run build:rust && pnpm run build:ts && cp core/target/release/spire-rust extension/bin/",
    "dev": "pnpm run build && code --extensionDevelopmentPath=./extension --disable-extensions",
    "test": "pnpm run build:ts && cd extension && npm run test",
    "package": "pnpm run build && cd extension && vsce package"
  },
  "devDependencies": {
    "@types/vscode": "^1.90.0",
    "typescript": "^5.4.0",
    "vsce": "^2.15.0"
  }
}
